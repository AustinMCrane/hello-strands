# Base URL for the OpenAI-compatible endpoint (required)
OPENAI_BASE_URL=https://your-endpoint.example.com/v1

# API key for the endpoint (required if the endpoint enforces auth)
OPENAI_API_KEY=your-api-key-here

# Model identifier to use (optional, defaults to gpt-4o)
MODEL_ID=gpt-4o
